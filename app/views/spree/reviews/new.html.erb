<section class="product-review-wrap py-4 py-md-5 py-lg-6">
  <div class="container">
    <%= content_tag :h3, Spree.t(:leave_us_a_review_for, name: @product.name), class: 'new-review-title' %>
    <%= render 'form', review: @review, product: @product %>
  </div>
</section>
